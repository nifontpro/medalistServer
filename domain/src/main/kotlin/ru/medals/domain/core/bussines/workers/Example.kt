package ru.medals.domain.core.bussines.workers

/*

//Ветвление любой сложности

val chain = rootChain<MyContext> {
	chain {
		on { condition == 0 }
		worker() {...}
		worker() {...}
		chain {
			on { subCondition == 1 }
			worker() {...}
			worker() {...}        }
	}
	chain {
		on { condition != 0 }
		worker() {...}
		worker() {...}
	}
}*/
